# PROJECT_STATE — Focus Mode App (đọc file này TRƯỚC)

> Bản "trạng thái chuẩn" (single source of truth) để bất kỳ ai — kể cả một phiên Claude mới — nắm toàn bộ project từ đầu tới hiện tại rồi tiếp tục phát triển. Cập nhật: **2026-07-19** (changelog mục 32 — User Guide + fix phòng thủ bug email worklog + Feedback user/CMS admin; migration `00017` CHƯA chạy thật).
>
> Cặp đôi với file này: **`Plan_and_Deploy.md`** (lộ trình deploy cloud/AWS + lịch 2 tuần + diagram + PPT + test plan). Các file `docs/` khác được đồng bộ code ngày 2026-06-29; các thay đổi tính năng ngày 2026-07-05 (đợt rà local+DB) có thể CHƯA phản ánh hết vào chúng → **file này (PROJECT_STATE) là nguồn chuẩn** khi có mâu thuẫn.

---

## 0. Một phút nắm bắt
- Sản phẩm: **"Focus Mode App"** (tên hiển thị "FCAJ Worklog Repository") — app quản lý phiên tập trung (Pomodoro) + task + nhật ký cảm xúc + gợi ý nội dung (RAG), kèm trợ lý AI tạo task.
- Kiến trúc: **Nuxt SPA** (`ssr:false`) ở `web/`, **Supabase** (Postgres + Auth + pgvector) làm backend **cloud-only**, **AWS Lambda + API Gateway + Bedrock** (`aws/`) cho AI — **4/6 lambda đã deploy & live** (agent-bff, agent-action-handler, ambient-audio-manager, emotion-detector + Bedrock Agent Haiku 4.5), 2 lambda còn lại (rag/vectorizer) chưa code.
  - ⚠️ **Phiên bản Nuxt thực tế:** `package.json` khai `nuxt ^3.15` (**Nuxt 3**) dù nhiều doc/tiêu đề gọi "Nuxt 4". Khi build/deploy, coi là **Nuxt 3** (SPA static bằng `nuxt generate`).
- Giai đoạn hiện tại (2026-07-12): **app + DB + AWS core đã chạy thật trên production** (Amplify live, migration `00001`–`00014` đã chạy xong). Chỉ còn thiếu **2 AI lambda** (rag/vectorizer) — xem roadmap §5.
- Thư mục: `web/` (frontend) · `supabase/` (DB + migrations + RLS) · `aws/` (lambda/infra spec) · `docs/` (tài liệu).

---

## 1. Kiến trúc & sự thật hiện hành

### Cloud-only — offline-first ĐÃ GỠ
- KHÔNG còn offline-first, IndexedDB, Dexie, sync queue, Last-Write-Wins, mock backend.
- Đã xóa: `web/lib/db.ts`, `web/composables/useSyncQueue.ts`, cờ `NUXT_PUBLIC_USE_MOCK_BACKEND`, mọi mock/OTP in-memory.
- `web/composables/useOffline.ts` chỉ còn là chỉ báo kết nối (`navigator.onLine` → Online/Offline + 1 toast dashboard). `SyncStatus.vue` chỉ hiển thị Online/Offline.
- Mọi read/write đi thẳng Supabase qua `web/composables/useDataService.ts` và Pinia stores.

### Pinia stores thực sự tồn tại
- `web/stores/task.store.ts` (id `'task'`, cloud Supabase trực tiếp; có luồng review + khóa focus).
- `web/stores/focus.store.ts` (Pomodoro timer).
- `web/stores/user.store.ts`.
- KHÔNG có: `sync.store`, `dashboard.store` (dashboard tính inline), `media.store` (admin/media dùng `useDataService`), `auth.store` (auth là composable `useAuth.ts` + Supabase Auth).

### Auth & duyệt user
- Supabase Auth email/password. Đăng ký → trigger `handle_new_user()` tạo `public.users` với `status='pending'` (admin auto `'approved'`).
- Duyệt user bằng cột `public.users.status` (pending|approved|rejected) + RLS `is_admin()`. Admin approve/reject/re-approve/set-pending = `UPDATE users.status` (`useAuth.ts`; UI `admin/users.vue` có mục Pending/Approved/Rejected).
- **Admin = CHỈ `public.users.role='admin'`** — ĐÃ GỠ env `ADMIN_EMAILS` (frontend không còn override qua email). Tài khoản phải có `role='admin'` trong DB mới vào được `/admin`. Ai được admin khi đăng ký = danh sách email hardcode trong trigger `handle_new_user()` (xem `00008`).
- `app.vue` gọi `syncSession()` lúc mở app → re-validate role/status với DB (bị demote/reject/hết hạn thì tự logout).
- ⚠️ **Config Supabase BẮT BUỘC: "Confirm email" phải TẮT** (Authentication → Providers → Email). Cổng chặn là admin-approval (`users.status`); nếu bật Confirm email thì user được duyệt vẫn kẹt lỗi *"Email not confirmed"*. (Xác nhận user cũ đang kẹt: `UPDATE auth.users SET email_confirmed_at=now() WHERE email_confirmed_at IS NULL;`)
- **ĐÃ GỠ luồng cũ (migration 00007):** bảng `public.user_requests`, edge function `supabase/functions/approve-user`, bảng `public.profiles` (chưa từng tồn tại). Tuyệt đối không mô tả/khôi phục các thành phần này.
- Migrations: `00001` schema · `00002`/`00004` seed admin · `00003` auth trigger · `00005` demo accounts (`admin@focusmode.app/admin123`, `user@focusmode.app/user123`) · `00006` users.status + is_admin() + RLS · `00007` drop legacy approval · `00008` đồng bộ admin list + backfill role='admin' · `00009` hardening DB (chặn tự đổi role/status bằng trigger, siết INSERT worklogs/stats, FK users→auth.users, drop sync_log, index users.status) · `00010` `tasks.completed_at` (mốc hoàn thành bất biến + trigger `set_task_completed_at` + index) → map worklog/thống kê/AI theo NGÀY HOÀN THÀNH, không theo `updated_at` · `00011` trigger `sync_user_email` đồng bộ `public.users.email` theo `auth.users.email` khi user đổi email (mirror cho admin list) · `00012` RPC `change_my_email()` — đổi email NGAY (không cần confirm; frontend gọi `supabase.rpc('change_my_email')` thay cho `auth.updateUser({email})` vốn bị pending ở một số project) · `00013` bảng `ambient_sounds` (nhạc nền Admin quản lý: name + S3 url + is_active + sort_order; RLS đọc-all cho authenticated, ghi cho `is_admin()`; trigger `updated_at`).
- **Trạng thái migration thực tế:** ✅ **Đã chạy hết tới `00014`** trong Supabase SQL Editor, xác nhận hoạt động ổn định (2026-07-11) — RLS hardening + FK (`00009`), `completed_at` (`00010`), trigger đồng bộ email (`00011`), RPC `change_my_email` (`00012`), bảng `ambient_sounds` (`00013`), chat AI + rate-limit (`00014`). `sync_log` đã bị drop theo `00009`. Lịch sử chat lưu được, rate-limit `AGENT_DAILY_LIMIT` đang chặn đúng.

### DB tables (schema `public`)
`users` · `tasks` (status: pending|in_progress|completed|cancelled; priority 0–3; duration_spent; due_date; review; **completed_at** — mốc hoàn thành BẤT BIẾN, trigger giữ, dùng để map theo ngày) · `focus_sessions` (emotion_label, emotion_confidence, journal_text, duration_planned/actual, ambient_track) · `daily_worklogs` · `daily_stats` (chỉ `created_at`, KHÔNG có `updated_at`) · `media_library` (type CHECK **5 giá trị**: quote, sutra, video, article, audio; `embedding_vector VECTOR(384)`; index ivfflat cosine) · `ambient_sounds` (nhạc nền: name, url S3, is_active, sort_order; RLS đọc-all + ghi is_admin) · `sync_log` (đã drop ở 00009). Extensions: uuid-ossp, pgcrypto, vector. Functions: update_modified_column, search_similar_content, get_user_streak, is_admin, handle_new_user.

### Pages (`web/pages`)
`/` (landing, cloud-only — đã bỏ quảng cáo offline; có "Cloud Sync") · `/login` (Sign In / Request Access / Forgot) · `/dashboard` · `/tasks` · `/focus` · `/agent` · `/calendar` (History heatmap) · `/profile` · `/admin`, `/admin/users`, `/admin/media`, `/admin/ambient` · `/author`. Middleware: `auth` + `admin`.

### Tasks (hành vi UI)
- 3 section màu: **Pending (trắng/xám)**, **In Progress (đen/vàng)**, **Completed (trắng/xanh)**; đã bỏ tab "All".
- Tick hoàn thành → hiện hộp review **"How was this task?"** trước khi đánh dấu xong (luồng ở `task.store.ts` + component dùng chung `web/components/TaskReviewDialog.vue` đặt trong layout → chạy cả ở trang Tasks lẫn widget dashboard).
- CRUD Edit/Delete từng task. Task **Completed** → Edit chỉ sửa được `review`, các field khác khóa.
- Ô **Due date đã bỏ khỏi form tạo** (cột `due_date` vẫn giữ trong DB/model).
- Task tạo mới (tay + Agent) đều `status='pending'`.
- Tick bị **khóa 🔒** khi task đang gắn với phiên focus đang chạy/tạm dừng.

### Focus timer (hành vi hiện tại)
- Đếm ngược **neo theo mốc thời gian thực** (`endAt = Date.now()+duration`); chính xác kể cả khi tab chạy nền (tự hiệu chỉnh mỗi tick + listener `visibilitychange`).
- Pause/Resume; hết giờ → phát **chuông WebAudio + Notification trình duyệt** (xin quyền lúc Start; bấm thông báo để quay lại web), rồi Session Complete → journal → nhận diện cảm xúc → gợi ý nội dung → lưu phiên.
- CHỈ task `in_progress` mới gắn được vào phiên focus. Lưu snapshot `taskTitle` để hiển thị bền vững.
- **Nhạc nền (Ambient) — DB-driven (2026-07-08):** danh sách nhạc do Admin quản lý ở bảng `ambient_sounds` (trang `/admin/ambient`). `AmbientPlayer.vue` nạp track active → phát **file MP3 thật** từ S3 URL bằng `HTMLAudioElement` (loop + fade), thay cho nhạc synth WebAudio cũ. `focus_sessions.ambient_track` lưu URL track đã chọn.

### AI features (CHƯA hoàn chỉnh — cần `NUXT_PUBLIC_API_GATEWAY_URL`)
- **Agent chat**: `useAgentChat` → POST `{API}/agent/chat` → Lambda BFF → Bedrock Agent → action-handler lambda (CRUD task Supabase). Lambda `agent-bff` + `agent-action-handler` **CÓ code Python**. Chưa cấu hình URL → báo lỗi (không có mock).
- **Emotion**: `useEmotionDetector` gọi `/emotion` (kèm `Authorization: Bearer`) nếu có API, không thì fallback regex client. Lambda `emotion-detector` **ĐÃ DEPLOY & LIVE (2026-07-12)** — DistilBERT ONNX đóng gói trong Lambda, test `curl` với token thật trả 200, CloudWatch không lỗi.
- **RAG**: `useRAG` gọi `/rag` nếu có, không thì fallback hardcode. Lambda `rag-recommender` **chỉ README**. KB lưu trong Supabase (`media_library` + pgvector); `search_similar_content()`. Model `all-MiniLM-L6-v2` 384 chiều.
- **Report**: `useReportExport` gọi `/report` nếu có, không thì tải `.md` client. Lambda `report-generator` **chỉ README**.
- **Embedding**: `useDataService.generateEmbedding`/`generateAllEmbeddings` → `/embed`, `/embed-all`. Lambda `admin-vectorizer` **chỉ README**.

### AWS status
- **Bedrock Task Agent ĐÃ DEPLOY & LIVE (2026-07-08)** — chuỗi `POST /agent/chat` → `agent-bff` → Bedrock Agent → Action Group → `agent-action-handler` → Supabase `tasks`. ID thật: **agent `KKJCF9RAKJ`** · **alias `prod` = `K8YDCGJRW4`** (trỏ version 2, instruction ASCII sạch) · **guardrail `9l9zw1sh1tei` v1** (Prompt Attack HIGH + denied topics) · **action group `80WEPXLIJ8`** (todo-manager-api) · **model `global.anthropic.claude-haiku-4-5-20251001-v1:0`** (Claude **Haiku 4.5**, global cross-region inference profile — **50 RPM**, gấp 10× Sonnet 3.5 V2 apac=5 RPM). Chọn Haiku 4.5 vì quota cao nhất trong model account có access → **hết `throttlingException`** cho multi-turn; rẻ/nhanh, đủ cho task agent. **Finding quan trọng:** dùng **global** inference profile cho Agent cần agent service role có **`bedrock:GetInferenceProfile`** (+ `GetFoundationModel`) — KHÔNG chỉ `InvokeModel`; thiếu nó → `AccessDenied ... using InferenceProfile global.*` khi update-agent (đây là lý do trước đó tưởng global bị chặn). *(Lịch sử: Haiku 3 account không có access; Sonnet 3.5 v1/v2 apac chỉ 2/5 RPM.)* Nếu cần hơn 50 RPM: Service Quotas → Bedrock → "Global cross-region ... Claude Haiku 4.5 requests per minute". · agent service role `AmazonBedrockExecutionRoleForAgents_task` (đã thêm `s3:GetObject` để đọc action-group schema từ S3 lúc runtime) · lambda role `focus-ai-lambda-role`. Route trên HTTP API `ffepnb6gei` (integration `qaf0g6v`, route `r5uwuyl`); test không token → 401 (đúng). `agent-bff` code: auth in-Lambda Supabase-validate ES256 + sessionId namespace chống hijack + cap input + try/except+CORS. `agent-action-handler`: parse cả `requestBody` (fix bug P0 mất title/description) + fail-closed userId + whitelist field + delete 404 + apiPath passthrough. **Còn lại (frontend):** set `NUXT_PUBLIC_API_GATEWAY_URL=https://ffepnb6gei.execute-api.ap-southeast-1.amazonaws.com` (Amplify + `web/.env`) rồi test `/agent`. Runbook: **`aws/bedrock/DEPLOY-cmd.md`**; cập nhật sau này: **`aws/UPDATE-guide.md`**; các phát hiện/gotcha khi deploy (quota throttling, ES256, model access...): **`docs/bedrock-agent-findings.md`**.
- Lambda **ĐÃ DEPLOY & LIVE**: `ambient-audio-manager` (2026-07-08) — S3 presigned upload + ListObjectsV2 cho Ambient Sound. Đã tạo: bucket `focus-mode-ambient-audio` (public-read + CORS, 5 MP3), IAM role `ambient-audio-manager-role`, Lambda (python3.12), **API Gateway HTTP API** `ffepnb6gei` → endpoint `https://ffepnb6gei.execute-api.ap-southeast-1.amazonaws.com` (test GET/POST = 200). ⚠️ **Lambda Function URL `auth-type NONE` bị account trả 403** dù cấu hình đúng (không có org/SCP) → đã chuyển sang API Gateway. Quick-create HTTP API KHÔNG tự thêm quyền invoke → phải `add-permission` cho `apigateway.amazonaws.com` (nếu thiếu = HTTP 500). Presigned PUT phải ký bằng **endpoint regional** (`endpoint_url=https://s3.<region>.amazonaws.com` + virtual addressing) — endpoint global `s3.amazonaws.com` gây 301 redirect → browser upload "network error". **Bảo mật (ĐÃ bật admin-only):** API Gateway public nhưng Lambda tự xác thực trong `_authorize()` — **để Supabase validate token** (không tự verify chữ ký): lấy `sub` (decode payload), gọi `GET /rest/v1/users?id=eq.{sub}&select=id,role` **kèm token** → PostgREST verify token (mọi thuật toán) + RLS trả role; `role='admin'`→qua, sai/hết hạn→401, không phải admin→403. **Bật bằng env `SUPABASE_URL`+`SUPABASE_ANON_KEY`** (đã set); bỏ 2 biến này = public. `SUPABASE_JWT_SECRET` **không còn dùng** (thừa). **2 bug đã fix & document** (DEPLOY-cmd.md "Bug auth"): (1) token user là **ES256** không phải HS256 → không tự verify HS256 được; (2) admin đọc được nhiều dòng `users` (RLS) → phải lọc `?id=eq.{sub}` thay vì `rows[0]`. Runbook: `aws/lambdas/ambient-audio-manager/DEPLOY-cmd.md` (Bước 7 + Bug auth).
- Lambda **ĐÃ DEPLOY & LIVE**: `emotion-detector` (2026-07-12) — DistilBERT ONNX INT8 đóng gói thẳng trong Lambda (không qua Bedrock, không Lambda Layer — `onnxruntime`+`numpy`+`tokenizers`, ~204MB uncompressed/83.7MB zip), auth in-Lambda giống `agent-bff` (role `focus-ai-lambda-role`). Route `POST /emotion` trên HTTP API `ffepnb6gei` (integration `p2ikfo3`, route `u118lpg`). **Bug thật gặp lúc deploy** (đã fix trong `DEPLOY-cmd.md`): cờ `--platform manylinux2014_x86_64` đơn lẻ trong lệnh `pip install --target package` KHÔNG còn khớp wheel `onnxruntime`/`numpy` bản mới (đã đổi tag sang `manylinux_2_27/2_28_x86_64`) → `pip` báo `No matching distribution` và **không tạo thư mục `package\`**, khiến lệnh copy `lambda_function.py` sau đó lỗi "cannot find the path specified" và zip tạo ra thiếu cả code lẫn dependency; fix bằng cách truyền cả 3 `--platform` tag cùng lúc + cài `tokenizers` riêng với `--no-deps` (tránh kéo theo chuỗi `huggingface_hub`/`httpx`/`anyio` không cần thiết, ~30MB, vì code chỉ gọi `Tokenizer.from_file()` chứ không gọi `from_pretrained()`). Verify thật: `curl` không token → 401; với token thật → 200 + `{"label":...,"confidence":...}`; CloudWatch `/aws/lambda/emotion-detector` không có lỗi (cold start ~5.3s, warm 1.4-2.2s, peak memory ~275MB/512MB).
- Lambda **chỉ README (chưa code)**: `rag-recommender`, `admin-vectorizer` — **app vẫn chạy nhờ fallback client** (hardcode/lỗi im lặng). Kế hoạch implement chi tiết từng cái (dùng Bedrock để tránh ML layer): **`docs/ai-features-roadmap.md`**.
- Layers (`onnx-transformers`, `sentence-transformers`): chỉ spec. `aws/api-gateway/openapi.yaml` (đã thêm route `/ambient/*`; **authorizer trong spec KHÔNG dùng** — token ES256, auth in-Lambda) + `aws/bedrock/action-group-openapi.yaml`: có spec nhưng **CHƯA deploy**. IAM role JSON: `s3:ListBucket` trên `focus-mode-*`; `bedrock:InvokeAgent` scope `agent-alias/*` (đã sửa từ `agent/*`); đã bỏ `lambda:InvokeFunction` thừa. Chưa có CI/CD/IaC/WAF.
- **Chuẩn auth AWS (thống nhất):** token Supabase ký **ES256** → KHÔNG dùng JWT authorizer API Gateway (chỉ RS256). Mọi Lambda tự verify token in-Lambda: Bearer → decode sub → `GET /rest/v1/users?id=eq.{sub}` kèm token (PostgREST verify + RLS). Route admin thì Lambda check `role='admin'`. Frontend `useAgentChat.ts` đã sửa gửi `access_token` thật (trước gửi nhầm UUID) + bỏ `userId` khỏi body.

---

## 2. Đã làm trong giai đoạn này (changelog 2026-06-29)
1. **Fix focus timer**: đếm theo thời gian thực + chuông + Notification khi hết giờ (trước đây dùng `setInterval--` nên lệch khi đổi tab).
2. **Bug task gắn focus biến mất / "Task: None"**: lưu snapshot `taskTitle` vào focus store; hiển thị từ store.
3. **Khóa hoàn thành task** khi đang trong phiên focus (`isLockedByFocus` + `TaskLockedError`).
4. **CRUD task** (Edit/Delete) + dialog; bỏ ô Due date khỏi form (giữ cột DB).
5. **Review-on-complete dùng chung** ở mọi nơi (chuyển luồng vào `task.store` + `TaskReviewDialog.vue` trong layout).
6. **3 section màu** ở trang Tasks (bỏ "All"); **focus chỉ gắn task `in_progress`**.
7. **Task Completed** chỉ cho sửa `review`, khóa các field khác.
8. **Xác nhận**: Agent tạo task ở `status='pending'` (Lambda `agent-action-handler` hardcode).
9. **Dọn DB Phương án A**: migration `00007` drop `user_requests`; xóa edge function `approve-user`; cập nhật `00005` + `supabase/README.md`.
10. **media_type**: mở rộng frontend khớp DB (5 giá trị).
11. **Sửa marketing offline**: landing + dashboard không còn quảng cáo offline-first/IndexedDB.
12. **Đồng bộ toàn bộ `docs/`** với code (11 file kỹ thuật; giữ nguyên 2 file đề bài RFP/Supply theo yêu cầu).
13. **Tạo `Plan_and_Deploy.md`** (đủ §1–16: audit `aws/` + lộ trình deploy Amplify + lịch 2 tuần + diagram + PPT + test plan).
15. **Dashboard analytics (2026-07-06)**: thêm 4 chart nhẹ (SVG/CSS thuần, 0 lib) — Focus time 7 ngày, Tasks hoàn thành 7 ngày (theo `completed_at`), Task theo status (donut), Task đang mở theo priority. Component: `ChartBars.vue`, `ChartHBars.vue`, `ChartDonut.vue`; tính client-side từ tasks + focus_sessions.
14. **Rà & cải thiện tính năng local + DB (2026-07-05)**: sửa Ambient (phát audio thật bằng WebAudio + fix binding), verify mật khẩu cũ khi đổi, persist phiên focus qua reload + không mất journal khi save lỗi, khóa Delete task khi đang focus, sửa 3 lỗi stats dashboard (mood/tasks-done/streak), re-validate session lúc mở app (`syncSession`), thêm mục Rejected users, banner lỗi + Retry cho task list, Worklog gộp cả ngày chỉ có task. DB: migration `00009` hardening.
16. **Ambient Sound quản lý qua Admin (2026-07-08)**: trang `/admin/ambient` 2 phần — **Phần 1 S3 File Management** (upload MP3 lên S3 qua presigned URL + list file bucket, có Copy link / "Dùng ↓") dùng Lambda mới `ambient-audio-manager` (`POST /ambient/upload-url`, `GET /ambient/files`); **Phần 2 CRUD** bảng `ambient_sounds` = danh sách nhạc user thấy ở trang Focus. Đổi nhạc nền từ **synth → file MP3 thật** (`useAmbientSound` phát `HTMLAudioElement`, `AmbientPlayer` nạp từ DB). DB: migration `00013`. Frontend gọi backend qua biến riêng `NUXT_PUBLIC_AMBIENT_API_URL` (fallback `apiGatewayUrl`). **AWS backend ĐÃ DEPLOY & test 200** (bucket + Lambda + API Gateway `ffepnb6gei`, xem mục AWS status). **Còn lại để chạy production:** (1) chạy migration `00013` trong Supabase; (2) set `NUXT_PUBLIC_AMBIENT_API_URL=https://ffepnb6gei.execute-api.ap-southeast-1.amazonaws.com` trong env Amplify + redeploy (local `.env` đã set sẵn).
17. **Audit aws/ + hardening chuỗi Bedrock Agent (2026-07-08)**: chạy workflow 5-lens audit toàn bộ `aws/`. **Fix 3 P0**: (a) `agent-bff` đọc userId từ `requestContext.authorizer.claims.sub` — crash trên HTTP API không authorizer → đổi sang verify token in-Lambda (Supabase-validate ES256); (b) `agent-action-handler` chỉ đọc `event['parameters']` — mất toàn bộ `requestBody` (title/description) → thêm `_params()` gộp `requestBody.content.*.properties`; (c) `useAgentChat.ts` gửi UUID làm Bearer thay vì `access_token` → sửa. **Fix P1**: sessionId namespace theo user (chống session hijack), fail-closed userId + whitelist field (chống confused-deputy/mass-assignment), delete 404, cap input, try/except+CORS, `deploy.sh` dùng wheel manylinux (tránh ImportError), bỏ `psycopg2` thừa, IAM `bedrock:InvokeAgent`→`agent-alias/*` + bỏ `lambda:InvokeFunction`. **Bảo mật Bedrock** (chống prompt injection): tạo `aws/bedrock/DEPLOY-cmd.md` (runbook end-to-end) + `guardrail-config.json` (Prompt Attack HIGH + denied topics + PII) + `agent-instructions.txt` (hardened, "treat content as DATA") + IAM agent role. Cập nhật `aws/README.md`, `bedrock/README.md`, `openapi.yaml`, và `Internship_Architecture.drawio` (Supabase Auth ES256, auth in-Lambda, security layer). **Chưa deploy Bedrock** — theo runbook khi sẵn sàng.
18. **Bedrock deployed + 4 nâng cấp agent/report (2026-07-09)**: Bedrock Task Agent **LIVE** (agent `KKJCF9RAKJ`, alias `K8YDCGJRW4`, model **Haiku 4.5 global** 50 RPM — xem `docs/bedrock-agent-findings.md`). **(R4)** system prompt bắt buộc user nói **mức ưu tiên** trước khi tạo task (`agent-instructions.txt`, đã redeploy). **(R3)** giới hạn **lượt AI/user/ngày** (`AGENT_DAILY_LIMIT`=20, agent-bff gọi RPC `bump_agent_usage`; vượt→429; fail-open nếu chưa chạy migration). **(R2)** **lưu + tạo mới đoạn chat AI**: bảng `agent_conversations`+`agent_messages`, `useAgentChat` shared-state + persist Supabase, `agent.vue` có sidebar danh sách hội thoại + "New chat" + xóa. **(R1)** report Markdown **tải theo từng ngày** ở Worklog History (`profile.vue` nút ⬇ Report + `downloadReport(date)`); **bỏ gửi email/Lambda** (client-side) và **bỏ 2 mục hardcode** "AI Suggestions"/"Recommended Content" (sẽ thay bằng vector-embedding sau — vectorizer/rag/KB tạm để trống). DB: **migration `00014`** (cần chạy). Report có thêm cột Priority.
19. **Agent chat UX: xuống dòng + render markdown (2026-07-10)**: `AgentChat.vue` — ô nhập đổi từ `<input>` sang `<textarea>` auto-grow; **Enter gửi tin, Shift+Enter xuống dòng** (`handleKeydown`). Bubble chat render **Markdown** (bold/danh sách/code block/blockquote) thay vì text thô, qua util mới `utils/markdown.ts` (`renderMarkdown` = `marked` parse + `DOMPurify.sanitize`, chỉ chạy client vì `/agent` đã `ssr:false`). Thêm dependency `marked` + `dompurify` (`web/package.json`). Verify bằng Playwright harness độc lập (không qua auth) — xác nhận Shift+Enter chèn newline không gửi, Enter gửi đúng 1 lần, markdown render đúng (bold/list/code), không lỗi console.
20. **Fix agent không xóa/update được task theo tên + không xử lý bulk "xóa hết" (2026-07-10)**: **Root cause KHÔNG phải** status pending (`_delete`/`_update` trong `agent-action-handler` không hề lọc theo status) — thật ra action group **chưa từng có thao tác đọc/list task**, nên agent không có cách resolve tiêu đề → `taskId` (chỉ có `create/update/delete-task`, quyết định có chủ đích trước đó để tránh over-permission — xem `DEPLOY-cmd.md` bảng bảo mật). Thêm nữa, câu chặn "refuse nếu nhắc tới 'tất cả user'" khiến model hiểu nhầm "xóa hết task" (ý: hết task CỦA CHÍNH user) thành vượt phạm vi. **Fix**: thêm action **`GET /list-tasks`** (đọc, luôn `.eq('user_id', user_id)` từ `sessionAttributes` — không phá mô hình bảo mật, cap `MAX_LIST=50`) vào `action-group-openapi.yaml` + `_list()` trong `agent-action-handler/lambda_function.py`; sửa `agent-instructions.txt` dạy agent **gọi `listTasks` trước** để resolve tên→ID trước khi update/delete, và làm rõ **"tất cả/hết" (không kèm danh tính khác) = tất cả task của chính user** (được phép, lặp gọi `deleteTask`/`updateTask` cho từng ID tìm được) — chỉ từ chối khi có **danh tính khác** được nêu. Đã **deploy AWS**: Lambda `agent-action-handler` (update-function-code), OpenAPI schema mới lên S3 + `update-agent-action-group`, `update-agent` (instructions mới), `prepare-agent` → version 10, `update-agent-alias prod` → version 10. **Gotcha phát hiện khi deploy**: `tar -a -cf x.zip .` trong Git Bash (GNU tar) KHÔNG hỗ trợ nén `.zip`, âm thầm tạo file sai định dạng → phải dùng PowerShell `Compress-Archive` (đã ghi vào `DEPLOY-cmd.md`). Chưa test lại bằng UI thật (cần user thử trong app).
21. **Fix reset-password link trỏ về localhost + không có trang xử lý (2026-07-10)**: `forgotPassword()` (`useAuth.ts`) gọi `resetPasswordForEmail(email)` **không có `redirectTo`** → Supabase fallback về **Site URL** trong Dashboard (mặc định `localhost:3000`) → user click link trong mail luôn bị đưa về `http://localhost:3000/#access_token=...`. Thêm nữa, **kể cả khi domain đúng thì app cũng chưa có trang nào đọc token đó** — recovery hash đáp xuống `/` (`index.vue`) vốn không parse hash, nên coi như mất hút (không phải 404, chỉ là trang landing bình thường với token nằm im trong URL). Biến `NUXT_PUBLIC_APP_URL`/`config.public.appUrl` đã tồn tại sẵn trong `nuxt.config.ts` + `.env.example` từ trước nhưng **chưa từng được dùng ở đâu** — đây chính là bước "23. Cấu hình... Site/Redirect URL... NUXT_PUBLIC_APP_URL" đã note trong `Plan_and_Deploy.md` nhưng chưa làm. **Fix**: (a) `forgotPassword()` giờ gọi `resetPasswordForEmail(email, { redirectTo: appUrl + '/reset-password' })`; (b) trang mới `pages/reset-password.vue` — tự đọc `window.location.hash` (access_token/refresh_token/type=recovery, vì `supabaseClient.ts` tắt `detectSessionInUrl`), gọi `setSession()` rồi `updateUser({password})`, xóa token khỏi URL bar ngay khi đọc xong (`history.replaceState`); route thêm vào `routeRules` (`ssr:false`), KHÔNG gắn middleware `auth` (phải công khai cho người chưa đăng nhập). Verify bằng Playwright thật trên dev server: link thiếu/sai token → hiện đúng thông báo lỗi + token bị xóa khỏi URL (kiểm qua `location.href` live, không dùng `page.url()` vì Playwright không refresh live theo hash). **CẦN LÀM THỦ CÔNG (không sửa được bằng code)**: (1) Supabase Dashboard → Authentication → URL Configuration → **Site URL** = `https://focusmode.click` (hoặc domain Amplify) + **Redirect URLs** thêm `https://focusmode.click/reset-password` (giữ thêm `http://localhost:3000/**` để test local); (2) Amplify Console → Environment variables → set `NUXT_PUBLIC_APP_URL=https://focusmode.click` (build-time, cần redeploy) — hiện đang mặc định `localhost:3000` nên **link vẫn sai cho tới khi set var này**.
22. **Quét + đồng bộ 13 README trong `aws/`+`supabase/` (2026-07-10)**: sửa 2 lỗi có thể gây hại nếu làm theo — `UPDATE-guide.md` ví dụ model `claude-3-haiku` (account KHÔNG có access) và lệnh `tar -a` (tạo zip sai định dạng, đã biết bug từ mục 20) — đổi đúng model Haiku 4.5 + PowerShell `Compress-Archive`. Cập nhật trạng thái deploy sai (`agent-bff`/`agent-action-handler` từ "blocked" → **DEPLOYED**) ở `aws/README.md`, `bedrock/README.md`, 2 README lambda. Sửa claim sai "dùng JWT authorizer" ở `ambient-audio-manager/README.md` + `api-gateway/README.md` (thực tế 100% in-Lambda auth ES256). Sửa `supabase/README.md`: migration `00001–00007` → thực tế `00001–00014`, bổ sung 6 bảng thiếu (`ambient_sounds`, `agent_conversations`, `agent_messages`, `agent_daily_usage`, `daily_stats`) + note `sync_log` là tàn dư offline-sync không còn code nào dùng.
23. **Cắt giảm scope theo quyết định user (2026-07-10)** — dự án là demo/presentation cho AWS FCAJ bootcamp, KHÔNG public commercial cho nhiều user, nên các hạng mục "best-practice cho production quy mô lớn" dưới đây **chủ động KHÔNG làm** (không phải thiếu sót — đừng đề xuất lại trừ khi scope đổi/scale lên >100 user thật):
    - **Xóa hẳn `report-generator`** (folder `aws/lambdas/report-generator/`, path `/report` khỏi `api-gateway/openapi.yaml`, mọi reference trong `aws/README.md`/`api-gateway/README.md`) — on-demand export đã thay bằng client-side Markdown (mục 18/R1), nightly-aggregate cũng không cần.
    - **Xóa JWT authorizer khỏi `api-gateway/openapi.yaml`** (`security:`/`components.securitySchemes.jwtAuth`/`x-amazon-apigateway-authorizer`) — chưa từng dùng được (token ES256), giữ lại chỉ gây hiểu nhầm.
    - **Bỏ kế hoạch Secrets Manager** cho `SUPABASE_SERVICE_ROLE_KEY` (`bedrock/DEPLOY-cmd.md` Bước 1 đổi thành ghi chú "không làm") — service_role key tiếp tục để plaintext env Lambda.
    - **Bỏ kế hoạch API Gateway throttling / WAF rate-based** — mọi mention (`DEPLOY-cmd.md`, `agent-bff/lambda_function.py` docstring, `ambient-audio-manager/DEPLOY-cmd.md`) đổi thành "future update khi scale >100 user thật". Chống DoS hiện tại chỉ dựa vào cap `inputText` + `AGENT_DAILY_LIMIT`.
    - **`emotion-detector` giữ nguyên kế hoạch ONNX** (KHÔNG đổi sang dùng Haiku qua prompt — đã hỏi, user chọn giữ) — chỉ ghi rõ trạng thái thật: fallback hiện tại là keyword-regex thuần client (`useEmotionDetector.ts`), không phải AI.
    - `rag-recommender`, `admin-vectorizer`, Lambda Layers, KB ingestion — **tạm chưa đụng**, giữ nguyên README kế hoạch.
24. **Dọn tiếp report-generator ở 6 doc ngoài `aws/` (2026-07-10)**: `docs/ai-features-roadmap.md`, `docs/api/lambda_contracts.md` (xóa hẳn spec LaTeX/Tectonic cũ + đánh số lại các mục), `docs/cloud-migration-plan.md`, `docs/testing-plan.md`, `docs/project-assessment.md`, `Plan_and_Deploy.md` — đều đã cập nhật (đa số theo convention "Update (date):" sẵn có của từng file, giữ nội dung gốc làm lịch sử thay vì xóa, vì `project-assessment.md`/`Plan_and_Deploy.md` là tài liệu đánh giá). **Phát hiện quan trọng**: `docs/Internship Documentation.md` (RFP gốc) yêu cầu cụ thể **tự động gửi email báo cáo mỗi đêm 23:59 qua Lambda+EventBridge+SES** (dòng 134-136, 162, 486, 558, 576) — quyết định bỏ `report-generator` (mục 23) tạo ra **gap thật so với RFP** (client-side download không tự động, không gửi email), đã ghi rõ trong `project-assessment.md` thay vì xóa im lặng. `Plan_and_Deploy.md` §14 còn 3 node Mermaid `report-generator`/`REP`/`L6` **chưa gỡ** (rủi ro vỡ cú pháp khi sửa tay không render lại) — coi là kiến trúc lịch sử.
25. **Thêm As-Built Architecture diagram vào `docs/Internship Documentation.md` (2026-07-10)**: theo yêu cầu trực tiếp của user (ngoại lệ cho quy tắc "không sửa RFP" — xem memory `feedback-keep-docs-synced`), thêm section mới **"# As-Built Architecture"** (sau 2 diagram gốc "Initial Design"/"Final Design", KHÔNG xóa 2 diagram gốc) gồm: bảng so sánh kiến trúc gốc (Flutter + Drift SQLite offline-first + self-host Docker trên HP Mini PC + Python NLP microservice + Lambda Report Gen/SES/SNS/CloudFront) vs thực tế đã build (Nuxt 3 SPA cloud-only + Supabase Cloud managed + 3 Lambda AWS thật + Bedrock Agent Haiku 4.5, không SES/SNS/CloudFront) + 1 diagram Mermaid mới vẽ đúng kiến trúc hiện tại. **Verify**: render thật bằng `mermaid` npm package + Playwright (browser thật, không đoán cú pháp) — `mermaid.render()` thành công, SVG 34KB, không lỗi console; đã xem screenshot xác nhận layout đọc được.
26. **Tạo slide thuyết trình + đính chính trạng thái dự án (2026-07-11)**: tạo `presentation/slides.md` (Marp) + export `slides.pptx`/`.pdf`/`.html` — 10 slide (giới thiệu → mục tiêu → công việc đã làm → kết quả → khó khăn/bài học → kết luận/hướng phát triển), nội dung lấy từ `PROJECT_STATE.md`+`bedrock-agent-findings.md`+`user-stories.md`(28/44 story ✅)+`project-assessment.md`(7.5/10)+RFP; đã render+chụp ảnh xác nhận cả 10 slide (không đoán cú pháp Marp). **Đính chính trạng thái** (user xác nhận trực tiếp, sửa các claim lỗi thời trong mục 3/4 cũ): (a) migration `00009`–`00014` **đã chạy xong, hoạt động ổn** (không còn "cần chạy"); (b) `amplify.yml` **CÓ tồn tại** nhưng nằm ngoài `code/` — repo này chỉ là sub-folder của 1 workspace tổng lớn hơn được push GitHub; (c) `.gitignore` **đã có** ở repo root, che đúng `web/.env` (dòng cũ nói "chưa có" là sai); (d) test coverage — user quyết định **để cuối cùng, không ưu tiên**. **Roadmap chốt** cho phần còn lại (xem mục 5 phía dưới): làm xong 3 AI lambda → sửa FE (vá auth header cho `useEmotionDetector`/`useRAG`/`useDataService` — **đã verify code hiện KHÔNG gửi `Authorization` header nào**) → viết full doc → làm slide → làm lại proposal → (tiện thì sửa luôn backlog polish).
27. **Fix 3 bug agent + ambient audio (2026-07-11)**: **(a) Description quá sơ sài** — agent tạo task chỉ có title, description trống → sửa `agent-instructions.txt` bắt agent tự phân tích + viết description 1-3 câu hướng dẫn thật, không phải nhắc lại title. **(b) Task lớn không tách sub-task** — user muốn "Làm CV" tách thành 6 task con (`Làm CV - <bước>` + description riêng từng bước) thay vì 1 task với description liệt kê gạch đầu dòng → sửa `agent-instructions.txt` dạy agent phân biệt goal đơn (1 task) vs goal nhiều bước (tách N task, mỗi task tự set lại priority/due date theo goal gốc). Verify qua rationale trace thật: model tự nhận diện "MULTI-STEP goal" và chọn đúng 6 bước tương tự ví dụ user đưa. **(c) Agent bịa năm sai cho due date** (đề 15/07 → hỏi 2024/2025, toàn năm đã qua) — **root cause**: đã inject `currentDate`/`currentDayOfWeek` vào SAI field `sessionAttributes` — field này CHỈ tới được Lambda qua `event`, model KHÔNG đọc được; field đúng là **`promptSessionAttributes`** (2 field tách biệt trong `invoke_agent` sessionState, xác nhận qua `botocore` service model). Sửa `agent-bff/lambda_function.py`: `userId` giữ ở `sessionAttributes` (đúng, để model không thấy/sửa được — chống confused-deputy), `currentDate`/`currentDayOfWeek` chuyển sang `promptSessionAttributes`. Verify qua rationale trace thật: model tự tính đúng "hôm nay 2026-07-11 → 20/07 chưa qua → dùng 2026-07-20". **(d) Ambient audio dừng khi đổi tab browser** — sửa `useAmbientSound.ts`: thêm auto-resume khi phát hiện browser tự pause `<audio>` ngoài ý muốn (lắng nghe event `pause` gốc + `visibilitychange` làm lưới an toàn thứ 2, phân biệt với `stop()` chủ động qua cờ `intentionalStop`) — chưa xác định được cơ chế browser chính xác gây dừng (không tái hiện được qua headless/synthetic test), đây là fix phòng thủ chuẩn cho triệu chứng này, cần user tự test tay để xác nhận. Đã deploy AWS: `agent-bff` (`update-function-code`) + agent instructions (`update-agent`→`prepare-agent`→`update-agent-alias`, version 14). Frontend (`useAmbientSound.ts`) đã sửa code + typecheck PASS + smoke-test `/focus` route không lỗi, **CHƯA commit/push**.
28. **Fix root cause thật của "ambient audio dừng" (2026-07-11)**: user làm rõ lại — vấn đề chính không phải đổi tab browser (cái đó vốn đã ổn), mà là **điều hướng SANG TRANG KHÁC trong app** (dashboard/tasks/agent) giữa lúc phiên Focus đang chạy làm nhạc dừng, và quay lại trang Focus thì nhạc bị phát lại từ đầu thay vì tiếp tục. **Root cause thật**: `pages/focus.vue` có `onBeforeUnmount(() => ambient.stop())` — component này bị Vue hủy mỗi khi điều hướng rời `/focus` (kể cả khi phiên vẫn đang `running`), nên `ambient.stop()` bị gọi bất kể lý do rời trang là gì. Trong khi đó `focus.store.ts` (Pinia store) sống độc lập, không phụ thuộc trang nào đang mount — nên logic phát nhạc lẽ ra phải đặt ở tầng tương đương, không phải trong 1 page component. **Fix**: chuyển watcher `focusStore.status -> ambient.play()/stop()` từ `focus.vue` sang `app.vue` (root layout, luôn mount, sống qua mọi điều hướng); xóa `onBeforeUnmount(() => ambient.stop())` + 2 lời gọi `ambient.stop()` thừa trong `saveJournal()`/`skipJournal()` (nay watcher toàn cục tự lo qua thay đổi `status`). Vì `focusStore.status` không đổi giá trị chỉ vì đổi trang, watcher KHÔNG fire lại khi quay về `/focus` -> nhạc chưa từng thật sự dừng, không có khái niệm "resume" hay "restart" nữa, đơn giản là **không bao giờ bị đụng tới** khi qua lại giữa các trang. Cơ chế auto-resume-khi-browser-tự-pause ở mục 27(d) vẫn giữ nguyên làm lưới an toàn phụ cho trường hợp browser/OS thật sự pause. **Verify**: typecheck PASS; thử test E2E thật bằng tài khoản demo `user@focusmode.app` nhưng **login thất bại "Invalid login credentials"** (tài khoản seed có thể không còn đúng như tài liệu cũ — cần user kiểm tra lại) nên chỉ verify được bằng đọc code + lý luận kiến trúc (Pinia store singleton + watcher ở root layout), CHƯA verify bằng browser thật. **CHƯA commit/push.** *(Chưa có xác nhận từ user rằng bug đã hết hẳn sau khi restart dev server + tab mới — cần theo dõi lại nếu user báo lại.)*
29. **Implement `emotion-detector` Lambda thật — DistilBERT ONNX (2026-07-12)**: theo quyết định user giữ nguyên ONNX thay vì đổi qua Bedrock Haiku (đã note ở mục 23). Viết đủ code trong `aws/lambdas/emotion-detector/`: `lambda_function.py` (handler thật — auth in-Lambda kiểu `agent-bff`, load model+tokenizer 1 lần lúc cold start bằng `onnxruntime`+`tokenizers`, KHÔNG dùng `transformers`/`torch` lúc runtime, map 6 nhãn gốc model → 5 nhãn app qua bảng xấp xỉ thủ công `MAP_TO_APP_LABEL` + heuristic confidence-thấp→`unmotivated`, stateless — không ghi Supabase), `prepare_model.py` (script export ONNX + quantize INT8 chạy 1 lần ở máy local, dùng `optimum.onnxruntime.ORTModelForSequenceClassification`), `test_local.py`, `requirements.txt`/`prepare-requirements.txt` tách runtime/export, `DEPLOY-cmd.md` (runbook Windows cmd đầy đủ — đóng gói qua S3 vì zip >50MB, reuse `focus-ai-lambda-role`, route `/emotion` trên HTTP API `ffepnb6gei` có sẵn), `README.md`. Sửa frontend: `useConfig.ts` thêm `emotionApiUrl`; `useEmotionDetector.ts` **vá lỗ hổng P0 đã note ở mục §3 cũ** — trước đó gửi request KHÔNG có `Authorization` header, giờ gửi `Bearer <session.access_token>` đúng pattern chuẩn, thêm nhánh `unmotivated` còn thiếu ở fallback regex. Đồng bộ route `/emotion` (bỏ `/emotion/detect`) ở `openapi.yaml`, `aws/README.md`, `docs/ai-features-roadmap.md` (đổi khuyến nghị "Bedrock Claude classify" — do chính AI đề xuất ở 1 turn trước — về lại đúng quyết định ONNX của user), `docs/nlp-emotion.md`, `docs/api/lambda_contracts.md` (contract cũ trong 2 file này sai nhiều: route `/emotion/detect`, request `{journal_text, session_id}`, ghi thẳng DB qua `psycopg2` — đã sửa khớp code thật `{text}`→`{label, confidence}`, stateless). **Verify:** `npm run typecheck` ở `web/` PASS. **CHƯA verify được**: việc export model thật (`prepare_model.py` chạy live) — sandbox chặn tải trực tiếp 1 model HuggingFace cụ thể (`bhadresh-savani/distilbert-base-uncased-emotion`) khi user chỉ nói "DistilBERT" chung chung, không nêu đích danh nguồn đó; đã verify riêng lẻ: cả bộ dep export cài+import sạch cùng nhau, model repo hợp pháp/an toàn (safetensors, apache-2.0, ~26k lượt tải), kích thước wheel runtime qua PyPI API — nhưng CHƯA chạy end-to-end thật. Đây đúng theo ý user ("để ta chạy chứ mi đừng có làm suông") — user tự chạy `DEPLOY-cmd.md` để deploy thật. **CHƯA commit/push.**
30. **`emotion-detector` deploy thành công + fix bug runbook + gitignore (2026-07-12)**: user tự chạy `DEPLOY-cmd.md` (mục 29) — dính 2 lỗi thật trong lúc chạy, cả 2 đều do runbook sai chứ không phải thao tác sai, đã chẩn đoán + tự tay sửa trực tiếp trên máy user (có quyền Bash/PowerShell cùng máy) thay vì chỉ hướng dẫn suông: **(a) Bước 3 đóng gói** — cờ `--platform manylinux2014_x86_64` không còn khớp wheel `onnxruntime`/`numpy` bản mới nhất (đã đổi tag phát hành sang `manylinux_2_27_x86_64`/`manylinux_2_28_x86_64`) → `pip install --target package` báo `No matching distribution` và im lặng không tạo thư mục `package\`, khiến lệnh `copy lambda_function.py package\` ngay sau lỗi "cannot find the path specified" và zip tạo ra **thiếu cả code lẫn dependency** (chỉ có file model) — build lại đúng bằng cách truyền cả 3 `--platform` tag + cài `tokenizers` riêng với `--no-deps` (bản mới kéo theo `huggingface_hub`+`httpx`+`anyio`+`fsspec`... ~30MB thừa, chỉ cần cho `from_pretrained()` mà code không dùng — đã verify bằng cách grep source `tokenizers` xác nhận không có `import huggingface_hub` nào ở Python layer, chỉ có trong chuỗi bytes của file `.so` biên dịch sẵn) → zip cuối 83.7MB (204MB uncompressed, an toàn dưới giới hạn 250MB Lambda). **(b) Bước 5b `create-route`** — user giữ nguyên placeholder `<IntegrationId>` (không thay bằng ID thật) → cmd.exe hiểu `<`/`>` là ký tự redirect I/O → "The syntax of the command is incorrect"; tra `IntegrationId` thật (`p2ikfo3`) qua `aws apigatewayv2 get-integrations` rồi đưa lệnh đúng cho user chạy → route tạo thành công (`RouteId u118lpg`). Đã sửa cả 2 bug trực tiếp vào `DEPLOY-cmd.md` (thêm cảnh báo + dòng troubleshoot) để không lặp lại. **Deploy xong, verify thật qua CloudWatch** (`aws logs get-log-events`): nhiều lượt invoke sạch, không exception, cold start ~5.3s / warm 1.4-2.2s, peak memory ~275MB/512MB; `curl` không token → 401, với token thật → 200. **Cập nhật `.gitignore`**: thêm `aws/**/venv*/` (venv_prepare nặng ~944MB) và `aws/**/model/` (chứa file `.onnx` ~67MB tải/export từ HuggingFace — không commit, đã ghi rõ trong README là tự chạy `prepare_model.py` để tạo lại); các artifact khác (`function.zip`, `package/`, `log.txt`, `__pycache__/`) đã có sẵn pattern từ trước, verify bằng `git check-ignore -v` xác nhận cả 6 đường dẫn nặng đều bị ignore đúng, chỉ còn source code thật (`lambda_function.py`, `prepare_model.py`, `test_local.py`, `requirements.txt`, `prepare-requirements.txt`, `DEPLOY-cmd.md`, `README.md`) nằm trong `git status` sẵn sàng commit. Đồng bộ toàn bộ trạng thái "CHƯA deploy" → "**ĐÃ DEPLOY & LIVE**" ở `aws/README.md`, `aws/lambdas/emotion-detector/README.md`, `docs/ai-features-roadmap.md`, `docs/nlp-emotion.md`, `docs/api/lambda_contracts.md`, và mục 1/3/5 phía trên trong chính file này. **`emotion-detector` = lambda AI thứ 4/6 deploy & live** (còn lại: `rag-recommender`, `admin-vectorizer`). **Chưa commit/push** — user chưa yêu cầu.
31. **`admin-vectorizer` + `rag-recommender` implement + deploy — 6/6 lambda AI xong (2026-07-12/13)**: 2 lambda cuối cùng, dùng Bedrock embedding thay vì đóng gói ML (theo `docs/ai-features-roadmap.md`). **Đổi model giữa chừng**: user chọn Titan Embed v2 lúc đầu, nhưng verify thật (`aws bedrock list-foundation-models --region ap-southeast-1`) phát hiện **Titan KHÔNG có ở `ap-southeast-1`** (chỉ Claude/Nova/Cohere) — đề xuất **Cohere Embed Multilingual v3** (`cohere.embed-multilingual-v3`) sau khi user hỏi về nó: có sẵn NGAY tại Singapore (không cross-region), cùng 1024 chiều (verify thật kể cả với input tiếng Việt), không cần Inference Profile (khác `cohere.embed-v4:0`) — đơn giản hơn Titan hẳn. Migration `00015` (VECTOR(1024) + rebuild index + đổi chữ ký `search_similar_content()`) không cần sửa gì vì chỉ đổi dimension, không gắn cứng model. **Thiết kế bảo mật**: cả 2 lambda dùng CHÍNH access_token của caller để đọc/ghi Supabase (không dùng `service_role`), để RLS (`media_write_admin`/`media_read_all`) làm lớp kiểm tra độc lập thứ 2 — giảm bề mặt secret so với pattern service_role thường dùng. `admin-vectorizer` batch `/embed-all` (1 lệnh gọi Cohere cho tới 50 item thay vì tuần tự). **3 bug thật gặp lúc user tự deploy, đều tự chẩn đoán qua CloudWatch/PostgREST trực tiếp thay vì đoán**: (a) `create-function` của `admin-vectorizer` — lệnh gõ đúng, deploy sạch. (b) `create-route` của `rag-recommender` — user để nguyên `<IntegrationId>` (lặp lại đúng lỗi mục 30(b), cmd.exe hiểu `<`/`>` là redirect) → tra ID thật (`4t1xtvh`) đưa lệnh đúng. (c) **Bug thật quan trọng nhất**: `/rag` trả 500 "Loi noi bo" — tra CloudWatch chỉ thấy `HTTP Error 400: Bad Request` (không đủ chi tiết) → tái hiện bằng curl PostgREST trực tiếp (chỉ dùng anon key, KHÔNG dùng token user thật — tránh lộ credential, một lần bị sandbox chặn vì nhúng token admin thật vào lệnh bash trước đó) → lộ ra lỗi Postgres thật `42804: Returned type double precision does not match expected type real in column 7` — **bug có sẵn từ `00001_initial_schema.sql`** (`similarity REAL` nhưng biểu thức `1 - (embedding <=> query)` trả `double precision`), mang nguyên sang `00015`, CHƯA từng lộ ra vì đây là lần đầu tiên RPC `search_similar_content()` được gọi thật trong lịch sử project. Fix bằng **migration `00016`** (ép `::REAL` tường minh, `CREATE OR REPLACE` không cần `DROP` vì signature không đổi) — user tự chạy, verify lại qua CloudWatch xác nhận invoke sạch không còn `ERROR`. Cải thiện thêm: `rag-recommender/lambda_function.py` giờ log nguyên response body của `HTTPError` (trước chỉ log `str(exception)` cụt lủn, mất công debug). **Vá nốt 2 composable FE còn thiếu `Authorization` header** (P0 cuối cùng, đóng hoàn toàn): `useDataService.ts` (`/embed`+`/embed-all`, vá sớm hơn kế hoạch gốc lúc admin-vectorizer vừa xong vì cần test UI Admin Media thật ngay) và `useRAG.ts` (`/rag`, vá cùng lúc coi rag-recommender xong) — cả 2 theo đúng pattern `getSupabase().auth.getSession()` → `Bearer <access_token>`, thêm `ragApiUrl` vào `useConfig.ts` (fallback `apiGatewayUrl`, giống `emotionApiUrl`). `openapi.yaml` đồng bộ route thật (`/embed`, `/embed-all`, `/rag`). **Cả 6/6 lambda AI giờ đã deploy & live, đủ frontend auth header thật** — không còn P0 nào mở. Typecheck PASS sau mọi lần sửa FE. **Chưa commit/push.**

32. **Chẩn đoán Task Assistant "Service Unavailable" + 4 feature mới (2026-07-19)**: user báo Task Assistant (`agent.vue`/`AgentChat.vue`) trả "Service Unavailable" khi lượt cuối 1 hội thoại dài yêu cầu xóa hàng loạt task `in_progress`/`pending` rồi tạo 5 task mới. **Chẩn đoán (chưa fix, chỉ giải thích cho user)**: `agent-bff` timeout 30s ([aws/bedrock/DEPLOY-cmd.md:65](../aws/bedrock/DEPLOY-cmd.md)) gọi `bedrock-agent-runtime.invoke_agent()` đồng bộ, chờ Bedrock Agent orchestration xong hẳn — lượt cuối buộc agent gọi `agent-action-handler` (timeout riêng 15s) **nhiều lần liên tiếp** (mỗi lần xóa/tạo 1 task là 1 tool-call + 1 lượt model suy nghĩ giữa các lần gọi), cộng dồn dễ vượt 30 giây; ngoài ra API Gateway HTTP API có trần cứng ~30s cho integration timeout nên chỉ tăng Lambda timeout không đủ — cần gộp batch tool-call hoặc đổi sang mô hình async mới fix tận gốc (chưa làm, ghi nhận là backlog). Message "Service Unavailable" hiện đúng nguyên văn vì `useAgentChat.ts` (dòng ~144-148) lấy `e?.data?.message` từ response lỗi và API Gateway trả JSON mặc định `{"message": "Service Unavailable"}` khi 503.
    - **Feature 1 — Trang User Guide**: `web/pages/guide.vue` (mới) — 5 section giải thích Dashboard/Focus/Tasks/Agent/Profile+Worklog bằng tiếng Anh (khớp văn phong UI hiện có), có nhắc luôn triệu chứng "Service Unavailable" ở mục Agent. Thêm link `Guide` vào `navItems` user thường trong `layouts/default.vue`.
    - **Feature 2 — Fix (phòng thủ) bug email cũ trong worklog download**: trace `useAuth.ts::updateAccount()` xác nhận code cập nhật `currentUser.value.email` + `persistSession()` đúng ngay khi đổi email — file cũ user chụp (`focus-report-2026-07-07.md`) nhiều khả năng là file tải TỪ TRƯỚC lúc đổi email (chênh 12 ngày so với hôm nay), không chắc là bug sống. Vẫn thêm hàm mới `refreshCurrentUser()` (đọc lại `public.users` rồi đồng bộ `currentUser`+localStorage) và gọi nó ở đầu `useReportExport.ts::downloadReport()` — đảm bảo report luôn build từ email/tên MỚI NHẤT trong DB bất kể nguyên nhân stale gì (tab khác, race condition...), thay vì tin vào state cục bộ.
    - **Feature 3 — Feedback trong account user**: migration mới `supabase/migrations/00017_feedback.sql` (bảng `feedback`: `user_id`, `message`, `status` `new`/`read`/`resolved`, RLS — user chỉ insert/select dòng của mình, admin `is_admin()` select/update/delete tất cả). Composable `web/composables/useFeedback.ts` (`submitFeedback`, `listFeedback`, `updateFeedbackStatus`). UI: thêm card "Send Feedback" vào `web/pages/profile.vue` (textarea + nút Send, hiện cho cả admin lẫn user thường).
    - **Feature 4 — CMS admin đọc feedback**: `web/pages/admin/feedback.vue` (mới) — bảng liệt kê feedback (join thủ công với `public.users` theo `user_id` để lấy email/tên, đúng convention không dùng embedded-select của project), badge trạng thái, nút "Mark read"/"Resolve" gọi `updateFeedbackStatus`. Thêm tab "Feedback" vào tab-bar của cả 4 trang admin hiện có (`index.vue`/`users.vue`/`media.vue`/`ambient.vue`) để điều hướng nhất quán — theo đúng convention `/admin/ambient` cũng KHÔNG có mặt trong `navItems` top-nav, chỉ trong tab-bar. Sau đó thêm luôn 1 box "Feedback" vào grid ở `/admin` (Overview) — cùng style icon-box với User Management/Media/Ambient, badge số "N new" nếu có feedback `status='new'` (gọi `listFeedback()` lúc mount, im lặng bỏ qua badge nếu lỗi). Sau đó theo yêu cầu user, **ẩn card "Send Feedback" khỏi admin** trong `profile.vue` (thêm `v-if="!isAdmin"`) — admin là người ĐỌC feedback qua CMS, không cần gửi; RLS phía DB không đổi, chỉ ẩn UI.
    - **Verify**: `npm run typecheck` PASS sau cả 4 feature (2 lần chạy, không lỗi TS). **CHƯA chạy migration `00017` thật trên Supabase** (user cần tự chạy như các migration trước), **CHƯA test UI bằng browser thật**, **CHƯA commit/push**.

---

## 3. Vấn đề đang mở / phải sửa trước khi deploy (từ audit — chi tiết & ưu tiên trong `Plan_and_Deploy.md` §4)
- ~~**P0 Auth**~~ — ✅ **Đã đóng hoàn toàn (2026-07-13)**: `useRAG.ts` (route `/rag`, chỉ cần user đăng nhập) và `useDataService.ts` (`/embed`+`/embed-all`, admin-only) đều đã gửi `Authorization: Bearer <session.access_token>`. Cùng với `agent-bff`/`ambient-audio-manager`/`emotion-detector` đã vá từ trước — **không còn composable nào gọi API Gateway mà thiếu auth header**.
- ~~**P0 Route mismatch**~~ — ✅ **Đã hết**: `openapi.yaml` đã đổi khớp đúng route frontend thật dùng (`/rag`, `/embed`, `/embed-all`, `/emotion`) — không còn lệch `/rag/recommend`/`/admin/vectorize` nữa.
- **Lỗ hổng RLS leo quyền** (user tự `UPDATE role='admin'/status='approved'`) — ✅ **Đã vá** ở **migration `00009`** (đã chạy) bằng **BEFORE UPDATE trigger** `guard_user_self_update()`.
- **Amplify**: `amplify.yml` **CÓ tồn tại** nhưng nằm **ngoài workspace `code/` này** (repo `code/` chỉ là sub-folder được push lên GitHub từ 1 workspace tổng lớn hơn) — không phải thiếu, chỉ không nằm trong phạm vi thư mục này. `.gitignore` **đã có** ở repo root, che đúng `web/.env`. CI/CD cũ (`docs/cicd-cloudflare-pages.yml`) vẫn sai nền tảng nhưng không chặn gì (site chạy Amplify thật rồi).
- **Cả 6/6 lambda AI đã deploy & live** (2026-07-13): `agent-bff`, `agent-action-handler`, `ambient-audio-manager`, `emotion-detector`, `admin-vectorizer`, `rag-recommender` + Bedrock Agent. Còn lại: 2 Lambda Layer spec chưa dùng (không cần nữa — cả `admin-vectorizer`/`rag-recommender` đều gọi Bedrock API, không cần layer) + KB ingestion pipeline tự động (bảng `media_chunks` + model đa ngữ + crawl, optional/advanced — xem `docs/ai-features-roadmap.md` mục 5, có note luôn bug thật về giới hạn 2000 ký tự/text đã gặp và fix tạm bằng truncate).
- **Test coverage gần như 0%** (`vitest`/`pytest` chưa cài thật) — theo quyết định user (2026-07-11): **để cuối cùng, không ưu tiên** trong đợt làm nốt này.
- **Reset-password chưa xác nhận hoạt động trên production** (code đã fix — mục 21): CẦN set thủ công trong **Supabase Dashboard → Auth → URL Configuration** (Site URL + Redirect URLs allowlist `<domain>/reset-password`) và **Amplify env `NUXT_PUBLIC_APP_URL`**; chưa có xác nhận đã làm.

---

## 4. Đang dở / chờ xử lý
- **`Plan_and_Deploy.md` đã đầy đủ §1–16** — gồm audit + lộ trình deploy + **§12 lịch 2 tuần** + §13 gói tài liệu nộp + §14 diagram Mermaid + §15 outline PPT + §16 kế hoạch test production.
- **Backlog polish (đợt rà 2026-07-05 — toàn 🟢, không chặn gì)**, dự kiến làm SAU khi xong AI (xem roadmap §5 dưới): `duration_spent` cộng dồn theo task · gom RLS admin về 1 nguồn `is_admin()` (00005 subquery vs 00006) · section "Cancelled" cho task · modal "New Task" chết ở dashboard · card System Health tĩnh · ESC/focus-trap cho modal · confirm khi Promote/Demote · đổi tên "Dominant Mood" · `maxlength` input · singleton phiên focus đa-tab · gộp seed 00002/00004.

---

## 5. Roadmap còn lại (chốt với user 2026-07-11) + cách tiếp tục
**Thứ tự làm nốt (theo đúng ý user, không đổi thứ tự trừ khi được yêu cầu):**
1. ~~**Làm xong 2 AI lambda còn lại**~~ — ✅ **Xong (2026-07-13, mục 31)**: cả 6/6 lambda AI đã deploy & live.
2. ~~**Sửa FE** liên quan~~ — ✅ **Xong** (mục 31): route mismatch hết, cả 6 composable đều gửi đúng `Authorization` header. *(UI polish lặt vặt nếu phát sinh thì coi như backlog ở mục 6.)*
3. ~~**Viết full doc**~~ — ✅ **Xong (2026-07-19)**: Tier 0-5 doc-consistency pass (commit `1617de2`) + doc cho 4 feature mới (mục 32, commit `98ac009`). Toàn bộ `docs/` đã khớp trạng thái deploy thật.
4. ~~**Làm slide thuyết trình**~~ — ✅ **Có bản mới (2026-07-19)**: user tự làm qua **Canva** thay vì Marp cũ (`presentation/slides.md`/`.pptx`/`.pdf` cũ đã bị **xóa** khỏi working tree) — bản mới là `presentation/Le Hoang Triet Thong - FCAJ - Focus Mode App.pdf` + `presentation/Canva Link.txt` + `presentation/Internship_Architecture.png/.pdf` cập nhật lại. **Chưa commit** — Claude chưa tự tay làm phần này, chỉ quan sát file trên đĩa, chưa đọc nội dung để xác nhận chất lượng.
5. **Làm lại proposal.** ← **ĐANG Ở BƯỚC NÀY** (bước cuối cùng còn mở trong roadmap kỹ thuật; chưa có file proposal nào trong repo).
6. *(Có thể tiện tay sửa luôn backlog polish ở mục 4 trong bước này.)*
**Test coverage (vitest/pytest) — để cuối cùng, không ưu tiên** trừ khi user đổi ý.

**Đọc theo thứ tự khi mở phiên mới:** file này (`docs/PROJECT_STATE.md`) → `Plan_and_Deploy.md` → tài liệu `docs/` liên quan tới việc đang làm.

**Nguyên tắc làm việc:** cloud-only (không offline); duyệt user qua `users.status`; khi đổi code thì cập nhật `docs/` + file này; trả lời người dùng bằng tiếng Việt, thuật ngữ kỹ thuật giữ tiếng Anh; xác minh tuyên bố bằng code (file:line) trước khi khẳng định.

**Prompt mẫu để mở phiên mới:**
> "Đọc `docs/PROJECT_STATE.md` và `Plan_and_Deploy.md` để nắm trạng thái dự án Focus Mode App. Tóm tắt cho tôi: kiến trúc, việc đã xong, và scope đang dở. Hôm nay tiếp tục: [việc bạn muốn]. Trước khi sửa code, xác nhận trạng thái với tôi."
