# ============================================================================
# Đã ngừng duy trì file này (2026-07-13).
#
# Trước đây đây là 1 bản OpenAPI spec RIÊNG, tách khỏi `aws/api-gateway/openapi.yaml`
# — 2 bản lệch nhau dần theo thời gian (bản này vẫn còn route đã xoá bỏ như
# `/reports/generate`, `/ai/suggestions`, và route/path cũ như `/emotion/detect`,
# `/admin/vectorize` — không route nào khớp hệ thống thật nữa).
#
# NGUỒN CHUẨN DUY NHẤT cho OpenAPI spec: ../../aws/api-gateway/openapi.yaml
# (được duy trì đồng bộ với code thật mỗi khi route/lambda thay đổi).
#
# Giữ file này lại (rỗng, chỉ có comment) thay vì xoá hẳn, để bất kỳ tài liệu/link
# cũ nào trỏ tới đây vẫn thấy được lời giải thích thay vì lỗi "not found".
# ============================================================================
